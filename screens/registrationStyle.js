
import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    titleReg: {
        textAlign: "center",
        // marginVertical: 20,
        marginTop:-32,
        fontSize: 30,
        lineHeight: 35.16,
      },
}

)

export const {
    titleReg,
} = styles;